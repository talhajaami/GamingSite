import {
  HomeDemo1Distribution,
  HomeDemo1Graph11,
} from "../../utils/allImgs"

import SectionHeading from "../SectionHeading"

const MintTokens = ({ data, ClassSpanTitle }) => {

  return (

    <section className="token-distribution dark-background section-padding-100-85">
      <div className="container text-white my-5">
      <h2 className='text-white text-center' data-aos="fade-up">Mint Tokens using NFT Machine</h2>

        
      </div>
    </section>
  );

}

export default MintTokens